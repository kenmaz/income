<%= form_for(@company_type) do |f| %>
  <% if @company_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@company_type.errors.count, "error") %> prohibited this company_type from being saved:</h2>

      <ul>
      <% @company_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
