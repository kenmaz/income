<%= form_for(@bonus) do |f| %>
  <% if @bonus.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bonus.errors.count, "error") %> prohibited this bonus from being saved:</h2>

      <ul>
      <% @bonus.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br />
    <%= f.number_field :amount %>
  </div>
  <div class="field">
    <%= f.label :age %><br />
    <%= f.number_field :age %>
  </div>
  <div class="field">
    <%= f.label :company_id %><br />
    <%= f.number_field :company_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
